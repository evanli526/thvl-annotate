# 当前机器复核包

包含450条任务、11,482个机器候选区间和机器转写，供本地标注程序自动展开。它们不是人工金标；保留缺媒体、partial和待复核项。

- `tasks.json.gz`：保留区间、类别、理由、证据和复核队列；本机绝对路径已脱敏，因此任务版本与原服务器版不同。
- `transcripts.json.gz`：界面所需的转写、声音描述和覆盖状态，不包含完整模型生成日志。
- `release.json`：压缩文件校验值、源任务哈希、固定数据集版本及448条媒体相对路径。

视频读取源为 `THVL/THVL-Bench` 的固定版本 `5ea20ec4074dea9d3419e88fea944313ab25818d`。程序仅按播放需要请求分段，不在本地缓存完整视频。云端不可达时保留错误，不将它解释为无风险。

原服务器人工结果不能直接混入本包；任务版本不同会被拒绝。请保留各版本原始工作目录，迁移时由负责人显式核对。人工结果和凭据均未包含在本包中。
